export enum ThemeMode {
  LIGHT = "light",
  DARK = "dark",
}

export enum ThemeLayout {
  VERTICAL = "vertical",
  HORIZONTAL = "horizontal",
}

export enum ThemeColorPresets {
  DEFAULT = "default",
}
